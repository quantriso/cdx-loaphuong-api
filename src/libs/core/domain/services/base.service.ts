import { Injectable } from '@nestjs/common';

/**
 * Base Service class
 * Provides foundation cho domain services
 */
@Injectable()
export abstract class BaseService {
  // Common service utilities và patterns
  // Subclasses can extend this for domain-specific services
  
}
